dependencies {
    // Exact, hash-verified development input only. This is deobfuscated for the
    // local development runtime and is never embedded in or published by Horizonwright.
    devOnlyNonPublishable(
        rfg.deobf(project.files("vendor/baritone/baritone-v1.2.19-mc1.7.10-1-7-10-forge+fcbbd4882c.jar"))
    )

    // The settings UI contract tests exercise the exact same pinned API. This
    // remains test-only and is still excluded from Horizonwright's artifact.
    testImplementation(
        rfg.deobf(project.files("vendor/baritone/baritone-v1.2.19-mc1.7.10-1-7-10-forge+fcbbd4882c.jar"))
    )

    testImplementation("junit:junit:4.13.2")
}
